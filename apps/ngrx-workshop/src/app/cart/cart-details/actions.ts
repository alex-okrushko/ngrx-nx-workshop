import { createAction } from '@ngrx/store';

export const pageOpened = createAction('[Cart Details Page] page opened');

export const purchaseSuccess = createAction(
  '[Cart Details Page] purchase success'
);
